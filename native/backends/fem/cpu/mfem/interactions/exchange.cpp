#include "cpu/mfem/interactions/exchange.hpp"

namespace fullmag::fem {
/*
 * Compatibility translation unit for the native FEM exchange aggregate include
 * surface. Operator assembly, field computation, runtime refresh, no-MFEM
 * fallback, mass projection, and legacy GPU upload live in dedicated modules.
 */

} // namespace fullmag::fem
