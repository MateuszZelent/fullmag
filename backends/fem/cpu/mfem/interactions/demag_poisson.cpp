/*
 * Poisson demag aggregate source contract.
 *
 * This source owns only the native Poisson-demag aggregate translation unit and
 * compatibility include surface. It does not assemble RHS, configure boundary policy, reduce periodic systems, solve Hypre, recover fields, compute energy, manage cache, postprocess fields, publish telemetry, or own lifecycle.
 */

#include "cpu/mfem/interactions/demag_poisson.hpp"

namespace fullmag::fem {

} // namespace fullmag::fem
