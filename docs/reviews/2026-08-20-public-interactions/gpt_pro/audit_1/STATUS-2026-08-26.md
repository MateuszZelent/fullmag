# Status remediacji audytu — 2026-08-26

Ten raport opisuje stan `master` po bieżącej serii remediacji audytu z 2026-08-26. Nie promuje żadnej capability GPU do `qualified`: testy źródłowe i kompilacja nie zastępują wykonanego receiptu sprzętowego, oracle na urządzeniu ani pomiaru hot-loop.

## Poprawki źródłowe

| Finding / obszar | Remediacja na `master` | Dowód lokalny | Pozostała granica |
|---|---|---|---|
| `FDM-CPU-PHY-001` | Energia DMI CPU jest składana na zorientowanych aktywnych ścianach; pola AoS, SoA, in-place i allocating dodają wariację brakujących ścian dla granic otwartych i masek. | Oracle energii ścian oraz test pochodnej kierunkowej dla iDMI/bulk DMI, granic otwartych i dziury maski; `cargo test -p fullmag-engine dmi --lib` — 17/17. | CUDA zachowuje osobny centered stencil; brak executed-device parity.
| `FDM-CPU-ARCH-001` | Reużywalny `IntegratorBuffers` wybiera jeden layout AoS/SoA na żywotność bufora i odrzuca zmianę capability typed `CapabilityUnavailable`; nie ma cichego przełączenia realizacji. SoA RHS stosuje tę samą końcową maskę frozen-spin co AoS, redukcja błędu wyklucza domenę nieaktywną i frozen, a regionalne napędy są składane identycznie w effective field, pełnym raporcie i gęstości energii. | `reusable_fdm_buffers_reject_layout_changes_without_mutating_state`; `soa_rhs_masks_frozen_spins_like_aos`; `finite_error_reduction_uses_active_free_cells_for_aos_and_soa`; `regional_drive_energy_is_reported_for_aos_and_soa_full_steps`; pełna grupa silnika — 355/355; wariant `parallel` — 355/355. | Brak pełnej macierzy qualification dla wszystkich backendów i integratorów.
| `FDM-CPU-NUM-001` | Wszystkie sześć produkcyjnych granic RK23/RK45 (AoS, buffer-SoA i persistent-SoA) korzysta z publicznego, wersjonowanego `AdaptiveStepController` ze stanem PI (`previous_error`) i bounded retry. Typed failure dla nie-finite error, `dt_min_exhausted` oraz retry-limit pozostają fail-closed; granica minimalnego kroku obejmuje także jeden ULP powyżej `dt_min` w CPU Rust i kanonicznym C++. Każdy rekord próby niesie wersję polityki, a runner publikuje ją w `solver_attempts.csv` i trace JSON. Redukcje normy błędu liczą maksimum po aktywnych, swobodnych komórkach. | `public_adaptive_controller_is_versioned_and_bounded`; `adaptive_controller_rejects_non_finite_and_stops_at_dt_min`; `adaptive_retry_budget_fails_with_a_bounded_terminal_record`; `adaptive_controller_treats_one_ulp_above_dt_min_as_exhausted`; `finite_error_reduction_uses_active_free_cells_for_aos_and_soa`; `fdm_adaptive_cpu` — 6/6; runner artifact pipeline — 15/15; pełna grupa silnika — 355/355; wariant `parallel` — 355/355. | Pozostaje pełna fault-injection macierzy checkpoint/FSAL/RNG dla każdej ścieżki produkcyjnej oraz niezależna kwalifikacja wydajnościowa; GPU i native FEM nie dziedziczą tej Rust CPU realization.
| `FDM-CPU-PHY-002` | Termika oraz lokalne człony CPU korzystają z per-cell `Ms`/`alpha`, z walidacją aktywnej maski i bez szumu w komórkach nieaktywnych. | Testy lokalnego szumu, pól AoS/SoA i direct torque w grupie silnika. | Brak statystycznego porównania CPU/GPU na urządzeniu.
| `FDM-CPU-TRX-001` | Retry i final-stage failure pozostawiają accepted state, historię i interwał RNG atomowe; ABM3 restartuje historię po zmianie kroku. Sprzężony ARS CPU ma osobny bounded retry, typed failure dla nie-finite LTE oraz ochronę postępu przy granicy `dt_min`. | `fdm_cpu_transactionality`, `fdm_adaptive_cpu`, `coupled_adaptive_controller_fails_closed_and_makes_progress`, testy ABM3 historii. | Brak pełnego fault-injection checkpoint/FSAL dla każdej ścieżki produkcyjnej.
| `FDM-CPU-NUM-003` | ABM3 pozostaje fixed-step: runtime resetuje historię po zmianie `dt`, a publiczna walidacja odrzuca adaptive ABM3 przed plannerem. | `abm3_restarts_history_before_using_changed_dt`; `adaptive_validation_rejects_fixed_step_abm3`. | Brak kwalifikacji prawdziwych współczynników variable-step i niezależnego testu rzędu.
| `FDM-CPU-NUM-002` | CPU FDM ma jawny `ProjectionPolicy::UnitSphere` dla wszystkich projected RK stage/candidate ścieżek (AoS, buffer-SoA i persistent-SoA) oraz stabilny realization ID `fullmag.fdm.rk_projection.unit_sphere.v1`; polityka jest teraz przenoszona przez Python authoring → IR → planner → `FdmPlanIR`, a CPU runner mapuje ją do typed engine policy i receiptu layoutu. | `fdm_projection_policy_round_trips_with_stable_realization_identity`; `fdm_projected_rk_policy_is_resolved_into_backend_plan`; `reference_runner_executes_and_provenances_resolved_projection_policy`; `projected_rk_macrospin_has_qualified_temporal_order` (Heun/RK23/RK4/RK45); `constant_field_live_soa_rhs_and_trajectory_match_analytic_llg`; Python round-trip `pytest` — 2/2; pełna workspace typecheck — PASS. | Pozostaje niezależny step-doubling/physics oracle dla projekcji, pre-projection defect/angle telemetry, fault-injection, parity GPU oraz pełna kwalifikacja projected RK; status nie jest `qualified`.
| `FDM-CPU-PERF-001` | Zwykły headless krok referencji wybiera `EvaluationRequest::Minimal`; pełne energie pozostają dla due scalar output, live update i relaxation z tolerancją energii. Finalny/outputowy snapshot nadal materializuje pełne obserwable. | Test wyboru polityki oraz 77/77 testów `fdm::cpu::reference::tests`; target Cargo poza repozytorium. | Brak pełnej telemetryki FFT/alloc dla wszystkich kombinacji outputu i integratora.
| `FDM-CPU-PERF-002` | Kopia `magnetization` do checkpointu jest tworzona tylko dla aktywnego transient spin transport; zwykły LLG nie alokuje jej przed każdym krokiem. Stałokrokowe persistent-SoA RK23/RK45 również wykonują etapy bez konwersji przez AoS, a RK45 buffer-SoA wypełnia istniejący cache FSAL in-place po warm-up. Publikacja persistent-SoA → accepted AoS ponownie wykorzystuje pojemności i kopiuje w każdym accepted step wyłącznie aktualną magnetyzację (`24 × N` B); cache FSAL i trzy sloty historii ABM3 pozostają authoritative w SoA i są synchronizowane do pełnego stanu transakcyjnego tylko raz przed końcowym digestem. Receipt `fullmag.fdm.cpu.step_transaction.v3` rozdziela liczbę i dokładne bajty publikacji per-step, końcowej synchronizacji, zaplanowanych artifact field snapshotów, payloadów live (magnetyzacja, preview i terminal cache) oraz field snapshotów materializowanych dopiero w finalizacji; liczniki są wyprowadzane z gotowych `Vec<f64>`, a deserializacja v1/v2 ustawia nieistniejące pola na zero. `StepStats` księguje odpowiadające bajty i czas materializacji w granicy kroku albo finalizacji. Fault-injection `NaN` w runtime external field dowodzi, że błąd RHS przed commit nie zmienia bitowego digestu authoritative persistent-SoA ani publicznego AoS mirroru i nie zwraca receiptu publikacji; po usunięciu faultu ten sam obiekt publikuje dokładnie raz `24 × N` B. Bezpośrednia ścieżka AoS ABM3 rotuje rozgrzane sloty bez `to_vec`. | `fdm::cpu::spin_transport::tests` — **28/28**; `rk45_soa_buffer_reuses_aos_fsal_storage_after_warmup`; `fixed_rk23_rk45_aos_and_soa_use_exact_dt_without_adaptive_suggestion`; `adaptive_cpu_meets_steady_state_allocation_and_rhs_to_accuracy_budgets`; `fixed_cpu_production_path_has_zero_steady_state_allocations_and_bytes` — Heun/RK4/ABM3 × Minimal/Full × 100 kroków, 0 alokacji i dokładnie jedna kopia `24 × N` B na krok, końcowy identyczny digest transakcyjny i analityczny oracle LLG, 32 kolejno zwolnione sesje bez retained-heap growth oraz 8 równoległych sesji z identycznym digestem, PASS default/`parallel`; `failed_persistent_soa_step_does_not_publish_or_mutate_accepted_state`; receipt v1/v2/v3 — **3/3**; `fdm::cpu::reference::tests` — **81/81**; pełny moduł `fdm::cpu::` — **138/138** w wariantach default i `fullmag-engine/parallel`, po zastąpieniu wadliwej absolutnej tolerancji kompozycji multilayer `H_eff` tolerancją absolutno-względną (czysty `origin/master` odtwarzał różnicę względną `1,54 × 10⁻¹⁶`); pełna grupa silnika default/`parallel` — po **356 passed, 1 ignored**. Szeroki `fullmag-runner --lib` nie jest zielony: **989/1010**, 21 awarii poza zmienioną granicą (m.in. fixture physics-graph, testy CUDA bez feature i wtórnie zatrute mutexy). | Pozostają OS-level peak RSS oraz hardware-specific memory-bandwidth/NUMA telemetry; finding nie jest jeszcze `qualified`.
| `FDM-CPU-PERF-003` | Publiczne `FDMDemag.fft_backend` jest transportowane przez Python → IR → planner → `FdmPlanIR`/`FdmMultilayerPlanIR` → aktywny runtime. Planner przed startem odrzuca niedostępne FFTW/MKL oraz niezgodne pary backend/device. CPU ma jednego właściciela selekcji bez zmiennej środowiskowej, a artefakty publikują typowany receipt requested/resolved/executed z polem wersji backendu, trybem planu, faktyczną liczbą wątków i layoutem workspace. Produkcyjna konwolucja CPU używa teraz półwidma: RealFFT wykonuje R2C/C2R w osi X, a cache'owane plany RustFFT wykonują osie Y/Z; trzy bufory rzeczywiste są współdzielone przez wejście M i wyjście H, sześć kerneli i sześć buforów roboczych mają rozmiar półwidma, a wszystkie linie i scratch są ponownie używane po warm-up. Publiczne pełne widmo kerneli wymagane przez istniejące ABI CUDA jest rekonstruowane z symetrii hermitowskiej i zweryfikowane względem poprzedniego layoutu. Single-grid CPU publikuje schema v2 z lifecycle revision/key, czasem setupu planu, zajętością własnych buforów, liczbami forward/inverse i skumulowanym czasem FFT. Niezależny naiwny DFT obejmuje parzystą i nieparzystą długość osi R2C, bezpośredni splot tensora Newella weryfikuje produkcyjne pole demag, a jawny release harness mierzy próg FFT względem splotu przestrzennego bez zapisu artefaktów do repozytorium. | `fdm_fft` — IR 3/3, planner 3/3, dispatch 2/2; CPU reference 77/77; kompilacja z feature `cuda` — PASS i CUDA provenance 4/4; Python DSL/UI — 23/23; `rustfft_r2c_demag_reuses_preallocated_workspace_after_warmup` — 0 alokacji w steady state, mniejszy workspace niż bazowe 12 pełnych buforów zespolonych i parity z bezpośrednim splotem; niezależny oracle R2C/DFT — PASS default/`parallel`; rekonstrukcja pełnego ABI kerneli — PASS default/`parallel`; testy FFT default i `parallel` — po 8/8; release benchmark na `70074ed95ec7c8906e3eb4cef0daf41f75dba34d` — break-even 64 komórki dla zbadanej rodziny siatek 2D; finalny receipt runnera publikuje `rustfft=6.4.1;realfft=3.5.0`, `realfft_r2c_planner_cached` i `half_spectrum_r2c`. | To częściowa remediacja: pozostają implementacje FFTW/MKL, równoważna telemetryka dla pozostałych realizacji, powtarzalny benchmark R2C/vendor na docelowych klasach sprzętu oraz pełna qualification.
| `FEM-CPU-NUM-001` | Referencyjne FEM CPU RK23/RK45 nie akceptują kandydata z błędem ponad tolerancję na `dt_min`; granica obejmuje również reprezentacyjne zaokrąglenie powyżej `dt_min`, a nie-finite norma kończy próbę typowanym błędem. Stan i czas pozostają niezmienione przed commit. Native `fill_common_step_metrics` redukuje `max_torque_Apm` wyłącznie po aktywnych, niefrozen węzłach, a pusty free-set daje zero. Adaptive RK korzysta teraz z jawnej mass-weighted RMS na lumped mass/node volumes i fail-closed przy braku lub uszkodzeniu miary. | `fem::tests::adaptive_rk_rejects_over_tolerance_at_dt_min_without_state_commit`; `fem::tests::adaptive_dt_min_boundary_accepts_rounding_but_not_a_real_larger_step`; `fem_adaptive_dt_contract` — weighted RMS, Airbox/frozen exclusion, invalid-measure rejection i norm receipt; managed `fem_rk_explicit_contract` — `FEM_CPU_NUM001_RECEIPT_PASS`; managed `fem_llg_time_domain_qualification` kompiluje publisher v2, ale runtime CPU zatrzymał się na istniejącym `post-relax demag residual exceeds qualification budget`; `just verify-fem-dg0-step-metrics-contract` — managed native PASS dla pinned/non-magnetic i all-frozen. | To częściowa remediacja: brak jeszcze jawnej requested/resolved norm policy w authoring/IR/planner, pełnej telemetryki angular guard/stop reason, CPU/GPU reduction parity, niezależnego oracle, refinement/time-to-accuracy benchmarku i pełnego managed runtime receipt.
| `FEM-CPU-NUM-002` | Planner FEM wylicza konserwatywną, typowaną estymację sztywności wymiany z rzeczywistego `h_min`, lokalnych ekstremów `A`/`Ms` oraz `gamma_mu0`; publikuje `omega_ex` i limit `dt` w provenance. Jawny krok ponad limit jest odrzucany w `strict`, a w `extended` pozostaje dopuszczony wyłącznie z jednoznacznym ostrzeżeniem i bez niejawnego przełączenia integratora. | `fem_exchange_stiffness_tests::exchange_stiffness_limit_scales_with_inverse_h_min_squared`; `fem_exchange_stiffness_tests::exchange_stiffness_uses_only_magnetic_cells_and_local_coefficients`; `tests::fem_exchange_stiffness_rejects_unsafe_fixed_step_in_strict_mode`; `tests::fem_exchange_stiffness_warns_for_unsafe_fixed_step_in_extended_mode`; `cargo test -p fullmag-plan fem_exchange_stiffness` — 4/4. | To częściowa remediacja: brak jeszcze automatycznego wyboru explicit/implicit/IMEX, implicit/IMEX lifecycle, niezależnego stability oracle, reject-ratio/linear-iteration telemetry, benchmarku refinement i time-to-accuracy; adaptive/nieoszacowane przypadki nadal wymagają osobnej kwalifikacji.
| `FEM-CPU-PHY-001` | Workspace DMI jest jedynym właścicielem walidacji i projekcji magnetyzacji do klas PBC; iDMI i bulk DMI korzystają z tego samego wejścia kanonicznego, a nie z dwóch niezależnych projekcji. Niepoprawny rozmiar AoS lub mapa klas kończy się typowanym błędem przed odświeżeniem GridFunction. | Managed `fem_dmi_contract` + `fem_dmi_weak_residual` w obrazie `fem-gpu` z `FULLMAG_ENABLE_CUDA=OFF` — `FEM_CPU_PHY001_PBC_DMI_PASS`; kontrakt źródłowy sprawdza jednego właściciela helpera i brak duplikacji polityki. | To częściowa remediacja: pozostają pełne material membership/quad contracts, Airbox zero-RHS/thermal oracle, PBC manufactured mode, fault-injection accepted state, CPU/GPU parity oraz performance/provenance qualification.
| `FEM-CPU-PERF-006` | Endpoint cache/FSAL jest używany tylko po elementowej zgodności znormalizowanego accepted candidate z ostatnim stage; rozbieżność wymusza endpoint refresh i unieważnia FSAL. Przy wyłączonym FSAL (Brown thermal) końcowa metryka RHS korzysta z `k[last]`. Receipt endpointu jest publikowany addytywnym, wersjonowanym ABI v1 i mapowany do kanonicznego `StepDiagnostics`. | `cpu_fsal_endpoint_cache_requires_matching_candidate_state`; `brown_thermal_final_rhs_uses_last_stage_without_fsal`; managed `fem_rk_explicit_contract` — PASS; layout FFI i mapowanie runnera — PASS. | Pozostają operator-count tests per tableau z rzeczywistym demagiem, niezależny endpoint physics oracle, test no-output oraz steady-state performance gate.
| `FEM-CPU-PERF-003` | Adaptive RK23/RK45 używa wyłącznie setup-owned `StepperWorkspace::attempt_checkpoint`. Hot path nie zawiera fallbacku ani konstrukcji checkpointu i kończy się fail-closed, jeżeli setup naruszył kontrakt. Reject unieważnia derived caches i warm-starty bez kopiowania O(N), a polityka profilera jest odczytywana przy capture/restore zamiast zamrażana podczas setupu. | RED: `FAIL: adaptive RK step execution must not allocate a compatibility checkpoint`. GREEN: managed `just verify-fem-gpu-rk-transaction-contract` — CTest **5/5**, `fullmag-fem-sys` **41/41**, runner check PASS, mixed-P1 rollback/transfer budgets PASS. Wymuszone retry potwierdza ten sam adres checkpointu, `attempt_cache_allocation_count=0`, snapshot/restore payload `0 B`, capture per attempt, restore per reject i zgodność endpointu z niezależnym oracle RK. | Remediacja źródłowa/testowa jest gotowa. Finding nie jest jeszcze globalnie `qualified`: brakuje hash-bound publicznego E2E/provenance oraz osobnego steady-state benchmarku reject overhead/time-to-accuracy na reprezentatywnej siatce.
| `FEM-CPU-PERF-002` | FEM CPU ma prealokowane accepted/trial generations dla magnetyzacji i publikowanych pól; commit/rollback używa `swap`, małe metadane zapisuje stałorozmiarowy journal, a retry unieważnia cache oraz warm-starty bez kopiowania O(N). Telemetryka ABI/runner/profile publikuje bytes, allocation count, latency i peak RSS. | Managed `just verify-fem-gpu-rk-transaction-contract` — 5/5 CTest; `fullmag-fem-sys` — 41/41; `cargo check -p fullmag-runner` — PASS; mixed-P1 PGBB/NCG `exchange_only` i `device_hypre` — PASS. Test 32 rollbacków: ten sam journal, 0 alokacji snapshotu CPU, 0 host snapshot/restore bytes. | Implementacja źródłowa jest gotowa, ale finding pozostaje niekwalifikowany: brak hash-bound publicznego E2E, szerokiej macierzy interakcji/integratorów i sprzętowego gate'u time-to-accuracy. Globalny licznik pozostaje 33/52.
| `FEM-CPU-PERF-004` | Consistent-mass exchange ma setup-owned `CGSolver` z trwałym `GSSmoother` i macierzą układu; operator jest tworzony raz, a każde zastosowanie ponownie używa solvera. PBC korzysta z precomputed reduced mass matrix oraz workspace RHS/solution/residual. Zerowe wiersze dla nieaktywnych węzłów są regularizowane tożsamością wyłącznie w macierzy solve, więc fizyczny `mass_form` pozostaje niezmieniony, a węzły Airbox otrzymują zero. | Zarządzany `fem_exchange_contract` — PASS, w tym projekcja consistent dla prism/pyramid/tetrahedron, domeny mieszanej i PBC oraz telemetryka reuse; w `just verify-fem-material-element-ms-contract` `fem_exchange_contract` przechodzi po kompilacji natywnej. | To częściowa remediacja: komponenty x/y/z nadal są rozwiązywane osobno; brak metryk iteracji/czasu/residual per stage, niezależnego physics oracle, benchmarku steady-state i pełnej kwalifikacji capability. Szeroki przepis zatrzymał się później na niezależnym strict GPU/Hypre teście demag.
| `FDM-GPU-NUM-001` | Embedded-error kernels RK23/DP45 liczą kanoniczną per-spin normę max z advanced `atol + rtol·max(‖m₀‖,‖m₁‖)` i wykluczają komórki nieaktywne oraz frozen przed redukcją. Ten sam przebieg per-cell wyznacza po projekcji `abs(‖m‖-1)` oraz geodezyjny obrót próby w radianach, a blokowa redukcja tripletu pozostaje na urządzeniu. Kontroler CUDA łączy trzy znormalizowane kryteria jako `max(embedded_error, norm_defect / norm_tolerance, spin_rotation / max_spin_rotation)`, zachowując surowy embedded error oddzielnie w telemetrii. Terminalny pakiet conditional graph przenosi wartości last/max bez dodatkowego D2H ani synchronizacji. Wersjonowane ABI v1 i typowany receipt/provenance publikują jawne identyfikatory semantyki, liczbę obserwacji, porównań z kanonicznym kontrolerem i rozbieżności decyzji. Planner zachowuje oba progi dla jawnego CUDA i odrzuca je fail-closed dla FDM CPU. | Managed `just verify-fdm-gpu-adaptive-control-contract` — CTest **3/3** na rzeczywistym CUDA oraz ABI/layout, planner, batched runner, mapowanie receipt i serializacja provenance — PASS. RK23/DP45 × FP32/FP64 zachowują identyczny trace i normalized error po replikacji siatki 1→8 oraz po dodaniu komórki nieaktywnej; FP64 potwierdza wykluczenie frozen. FP32 frozen pozostaje jawnie fail-closed jako niekwalifikowane. Conditional-graph device policy zgadza się z kontrolerem CPU dla `nextafter(1,0)`, dokładnie `1` i `nextafter(1,2)`, a syntetyczne przypadki guardów potwierdzają retry powyżej progu, akceptację dokładnie na progu i zachowanie surowego embedded error. Makrospin RK23/DP45 × FP32/FP64 wymusza retry przez guard obrotu, kończy poniżej progu i raportuje zero rozbieżności decyzji. Niezależny oracle Gilberta, NaN/`dt_min`/retry-limit i zero hot-loop control D2H przechodzą w tej samej bramce. | To częściowa remediacja: brakuje szerokiej, hash-bound kwalifikacji dla macierzy integrator/precision/interakcje oraz time-to-accuracy; capability pozostaje `unvalidated`.
| `FDM-GPU-ABI-001` | Wersjonowany descriptor v2 ma wspólny manifest 167 offsetów, generowane asercje Rust, C++ sentinel wszystkich pól i typed odrzucenie niezgodnej wersji/rozmiaru przed alokacją. Recepta managed rozdziela ścieżkę hosta WSL od stałej ścieżki kontenera. | `plan_desc_layout` — 2/2; `fdm_plan_desc_sentinel_contract` — managed CUDA PASS, checked-v2 Heun macrospin oracle, `executed_backend=cuda_fdm`, zero fallbacku; `cargo check -p fullmag-runner --features cuda` — PASS. | Pełny publiczny planner→runner E2E dla sentinelowej macierzy, hot-loop performance gate i szersza macierz interakcji pozostają otwarte; capability nie jest promowana.
| `FDM-GPU-ARCH-001` | Strict preflight i finalizacja odrzucają niepełne maski operatorów, host/unknown realization, fallback i pełne transfery lub host compute w hot loop. Managed receipt v2 jest publikowany atomowo poza checkoutem i wymaga pełnego commita oraz SHA-256 diffu względem `HEAD`. Recepta fail-closed potwierdza loop-backed ext4 zamiast efemerycznego overlayu Docker Desktop. | Managed CUDA `fdm_device_residency_receipt_contract` — PASS na RTX 3070 Laptop GPU; CTest 1/1; runner residency 13/13; routing 8/8; receipt `requested=gpu`, `resolved=device_resident`, `executed=cuda_fdm`, kompletne device masks, zero fallbacku, zero pełnych transferów i host compute w hot loop. Walidator odrzuca stale schema, brak provenance, host/unknown realization, fallback, zabronione transfery i niesklasyfikowaną synchronizację. | To częściowa remediacja. Brak publicznego Python→IR→planner→runner E2E, niezależnego oracle trajektorii dla tego receiptu, macierzy integratorów/interakcji i gate'u time-to-accuracy; capability pozostaje `implemented/unvalidated`.
| `FDM-GPU-PHY-001` | Single-grid i staged multilayer CUDA używają wspólnej korekty brakujących ścian iDMI/bulk DMI dla granic otwartych i masek, bez korekty na osi periodycznej. Multilayer receipt księguje faktycznie wykonane lokalne operatory i maski warstw. Komórkowe `Ms/A/alpha/Dind/Dbulk` pozostają jawnie fail-closed przed krokiem. | `just verify-fdm-gpu-dmi-boundary-runtime` na RTX 3070 Laptop GPU — CTest **2/2**: niezależny oracle pola i rzeczywisty krok FP64/FP32, single-grid oraz Heun/RK4/RK23 multilayer, kompletna device mask, zero fallbacku i pełnych transferów/host compute w hot loop; kontrakt pól materiałowych fail-closed. `just verify-fdm-gpu-abi-contract` — Rust **2/2**, sentinel **1/1**, runner CUDA check PASS. | Remediacja źródłowa/testowa jest gotowa, ale szeroka capability pozostaje niekwalifikowana: brak publicznego hash-bound E2E, energy/directional-derivative parity CUDA, sanitizera, łączonej macierzy interakcji oraz time-to-accuracy.
| `FDM-GPU-TRX-001` | Publiczne kroki single-grid i multilayer przechodzą przez wspólną granicę transakcyjną. Multilayer ma setup-owned snapshot magnetyzacji, staged commit czasu/`dt`/accepted index, bitowy rollback i odtworzenie obserwabli. Retry nie publikuje trial stats ani nie zużywa accepted RNG interval. | RED reproducer potwierdził, że fault final-stats wcześniej nie kończył transakcji błędem. Managed `just verify-fdm-gpu-transaction-contract` — CTest **3/3**, runner **2/2**, Heun/RK4/RK23 × FP32/FP64 × cztery fazy awarii, termiczny RK23/DP45 retry i bitowy checkpoint/restart deterministyczny oraz Brown thermal. ABI gate — Rust **2/2**, C sentinel **1/1**, runner check PASS. | Remediacja źródłowa/testowa jest gotowa. Compute Sanitizer pozostaje niezaliczony, ponieważ WDDM ma wyłączony systemowy interfejs debugowania NVIDIA; nadal brak szerokiej macierzy transport/interakcje, pełnego authoring→IR E2E i time-to-accuracy.
| `FDM-GPU-PERF-001` | Headless adaptive RK23/DP45 korzysta z transakcyjnego CUDA conditional-graph batch między granicami outputu i końcem stage. Decyzja PI, `dt`, retry i accepted-step sequence pozostają na urządzeniu; host odczytuje jeden terminalny pakiet na batch. Runner odświeża pełne obserwable wyłącznie na granicy publikacji. Interaktywny i relaxation lane pozostają jawnie na ścieżce per-step z pełnymi statystykami. | Managed `just verify-fdm-gpu-adaptive-control-contract` — natywne CTest **3/3**, runner batched E2E **4/4** oraz ABI/planner/provenance PASS. RK23/DP45 × FP32/FP64 przechodzą niezależny analityczny oracle Gilberta dla makrospinu, rollback po `dt_min_exhausted`, parity pola z legacy dla tego samego czasu i bramkę latency co najmniej 10% poniżej per-step; telemetryka dowodzi co najmniej 10× mniej terminalnych synchronizacji, zero hot-loop control-scalar D2H, zero fallbacku i realization `cuda_conditional_graph_batched_v1`. | To częściowa remediacja: brak czystego, hash-bound artefaktu rejestru dla dokładnego tuple integrator/interakcje oraz szerokiej macierzy demag/exchange/DMI/termika i time-to-accuracy. Capability pozostaje fail-closed `unvalidated`; finding nie jest jeszcze zamknięty.
| `FDM-GPU-NUM-003` | Centralna polityka FSAL nadaje termice i niezgodnym waveformom pierwszeństwo diagnostyczne także przy pustym cache; wszystkie cztery ścieżki RK23/DP45 FP32/FP64 dzielą ten kontrakt. | RED/GREEN `fdm_fsal_retry_transaction_contract`; managed CUDA `fdm_fsal_thermal_cuda_runtime_contract` — 12/12 przypadków, macrospin oracle, dokładnie 19/20 RHS saved, termika/waveform bez reuse, zero rollbacku/fallbacku. | Pełny Python--IR--runner E2E, stochastic-statistical qualification i produkcyjny time-to-accuracy pozostają otwarte; capability nadal `source_visible/unvalidated`.
| `FEM-GPU-ARCH-001` | Strict FEM CUDA FP64 publikuje wykonany receipt device-resident z pełną maską operatorów; fixture kwalifikacyjny porównuje teraz identyczną fizykę exchange + demag i wspólną politykę kierunku minimizatora po obu stronach. | `just verify-fem-llg-time-domain-qualification-production` — CPU PASS, GPU PASS, walidatory artefaktów PASS, parity PASS; `requested=strict_device`, `resolved=device_resident`, `executed=cuda_fem`, maska device `991`, host/unknown `0`, fallback `0`; różnica endpointu `2.9504581591051765e-16`. | Zakwalifikowano exact hardware tuple. Pozostają publiczny Python→IR→planner→runner E2E, szersza macierz interakcji/BC/integratorów i time-to-accuracy; finding pozostaje częściowo zremediowany.
| `FEM-GPU-PERF-001` | Audit compute hot-loop kończy się po obliczeniu RK, przed końcową materializacją statystyk; dozwolony odczyt obserwacyjny nie jest już błędnie klasyfikowany jako compute transfer/sync. | Ten sam managed hardware receipt: `hot_loop_compute_h2d_bytes=0`, `hot_loop_compute_d2h_bytes=0`, `hot_loop_compute_host_sync_count=0`, pełna device operator mask `991`. | Brak reprezentatywnego benchmarku steady-state/break-even i szerokiej macierzy; finding pozostaje częściowo zremediowany.
| `FEM-GPU-PERF-009` | Strict GPU wykonał demag RHS/solve/recovery przez device Hypre Poisson bez hybrid CPU round-trip. | GPU FP64 artifact PASS, CPU/GPU parity PASS, host operator mask `0`, fallback `0`, zerowe compute H2D/D2H w hot-loopie. | Pozostają pozostałe BC/PBC, reprezentatywne skale siatki i osobny performance gate; finding pozostaje częściowo zremediowany.
| `FEM` test contract | Test delegowania kompatybilnych integratorów normalizuje końce linii `include_str!`, więc nie zależy od LF/CRLF checkoutu. | `fem::tests::compatibility_integrators_delegate_to_workspace_path`; przechodzi w pełnej grupie silnika. | Zmiana dotyczy stabilności testu, nie dowodu wykonania GPU.

| `FEM-CPU-ARCH-001` | `AosVectorFieldView` oraz `PeriodicNodeMapView` rozdzielają lokalne węzły od okresowych true-node classes i wiążą mapę z rewizją. Frozen-spins jest importowane w rzeczywistej lokalnej przestrzeni `m_xyz`; klasy PBC wymagają identycznego membership/reference, a Airbox jest odrzucany. Wszystkie produkcyjne projekcje PBC są fail-closed. Typed audit liczy kopie reprezentacji, dokładne bajty gather/scatter, invalid-space assertions i część hot-loop. Wyłączone lokalne pola są zerowane przed kompozycją `H_eff`. Wersjonowany 88-bajtowy receipt v1 publikuje executed state/material spaces, local/true extents, rewizję mapy i liczniki do serializowanych `StepStats` oraz trwałego `StepDiagnostics`. | Managed `just verify-fem-cpu-representation-contract`: native CTest **13/13**, frozen C/Rust ABI layout **1/1**, runner mapping/corrupt/E2E **3/3**, przejście do `StepDiagnostics` **1/1**, `rustfmt` i pełny runner check — PASS; build na loop-backed ext4 poza checkoutem. | To nadal częściowa remediacja: brak pełnego local↔true round-trip przez MFEM GridFunction, CPU/GPU parity, fault-injection całego kroku i qualification/time-to-accuracy.

## Wcześniej wdrożone kontrakty GPU/FEM

| Obszar | Stan źródłowy | Czego nie należy jeszcze deklarować |
|---|---|---|
| FDM GPU ABI, residency, FSAL/transaction telemetry | ABI, receipty, fail-closed i telemetryka są obecne w historii `master`. | Brak aktualnego executed-CUDA tuple z parity, sanitizerem i time-to-accuracy.
| FEM GPU strict preflight | Publiczny `fullmag_fem_backend_step` uruchamia natywny `gpu_rk_prepare_step_preflight` przed `RkStepTransaction::begin`; `hybrid_cpu_poisson` jest odrzucany stabilnym błędem, a operator/device metadata są klasyfikowane jawnie. | Świeża recepta `just verify-fem-time-domain-native-contract` przeszła wszystkie natywne targety i `fem_gpu_strict_execution_contract`; nadal brak managed hardware receiptu, device Poisson oracle i pełnego pomiaru hot-loop.

## Regresja

- `just verify-fem-demag-poisson-contract-focused` — wszystkie sześć natywnych targetów Poisson/demag zbudowało się w kontenerze `fem-gpu` na ext4 poza checkoutem; wykonanie doszło do niezależnego mixed CUDA/Hypre i zakończyło się środowiskowym błędem `strict FEM GPU demag failed to select Hypre device memory`.
- `just verify-fem-demag-poisson-contract` — preflight zarządzanego runtime zatrzymał się wcześniej na istniejącym unsafe source symlink/junction `native/build`; nie jest to błąd zmian PERF-005.

Weryfikacja została wykonana z targetami Cargo poza repozytorium (dla tej remediacji `D:\fullmag-builds\audit-fdm-cpu-perf003`), więc buildy nie są indeksowane przez Git:

- `cargo test -p fullmag-engine --lib tests::` — **355 passed, 0 failed, 1 ignored**;
- `cargo test -p fullmag-engine --features parallel --lib tests::` — **355 passed, 0 failed, 1 ignored**;
- `cargo test -p fullmag-engine dmi --lib` — **17 passed, 0 failed**;
- `cargo test -p fullmag-engine --features parallel dmi --lib` — **17 passed, 0 failed**;
- `cargo test -p fullmag-engine --test fdm_cpu_transactionality --test fdm_aos_soa_parity --test fdm_adaptive_cpu` — **13 passed, 0 failed**.
- `cargo test -p fullmag-runner 'fdm::cpu::reference::tests' --lib` — **77 passed, 0 failed**.
- `cargo test -p fullmag-ir --lib` — **96 passed, 0 failed**.
- `cargo test -p fullmag-plan --lib` — **433 passed, 0 failed**.
- `cargo test -p fullmag-ir -p fullmag-plan -p fullmag-runner fdm_fft --lib` — **8 passed, 0 failed**.
- `cargo test -p fullmag-runner fdm_fft --lib --features cuda` — **4 passed, 0 failed**; jest to kontrakt źródłowy/runtime provenance, bez deklaracji wykonania na urządzeniu.
- `uv run --project packages/fullmag-py --no-sync pytest -p no:cacheprovider packages/fullmag-py/tests/test_fdm_multilayer_contract.py 'packages/fullmag-py/tests/test_fdm_ui_roundtrip.py::FdmUiRoundTripTests::test_scene_fdm_policy_exports_to_public_python_and_problem_ir'` — **23 passed, 0 failed**.
- `cargo test -p fullmag-engine --test fdm_fft_cpu_performance` — **1 passed, 0 failed, 1 ignored**; po warm-up konwolucja R2C wykonuje się bez alokacji sterty, ma mniejszy zmierzony workspace niż bazowe 12 pełnych buforów zespolonych i zachowuje parity z niezależnym bezpośrednim splotem Newella.
- `cargo test -p fullmag-engine fft --lib` oraz wariant `--features parallel` — po **8 passed, 0 failed**; osobny oracle `half_spectrum_3d_matches_direct_dft_and_normalized_round_trip` przechodzi dla parzystej i nieparzystej osi X w obu trybach.
- `FULLMAG_BENCH_COMMIT=70074ed95ec7c8906e3eb4cef0daf41f75dba34d FULLMAG_BENCH_BUILD_ID=windows-x86_64-msvc-release-rustfft-6.4.1-realfft-3.5.0 cargo test -p fullmag-engine --release --test fdm_fft_cpu_performance rustfft_r2c_demag_reports_direct_convolution_break_even -- --ignored --exact --nocapture` — **1 passed, 0 failed**; dla zbadanych siatek `2×2×1`–`32×32×1` pierwszy zmierzony punkt przewagi R2C wystąpił przy `8×8×1` (64 komórki): 11 804 ns wobec 16 192 ns bezpośredniego splotu. Dalsze wyniki R2C/direct: 2×2×1 — 1 304/89 ns, 4×4×1 — 3 793/1 106 ns, 16×16×1 — 42 130/256 020 ns, 32×32×1 — 165 750/4 331 650 ns. Jest to pojedynczy baseline tej maszyny, nie pełna qualification ani pomiar vendor FFTW/MKL.
- `cargo test -p fullmag-runner reference_runner_executes_the_planned_cpu_fft_backend_without_fallback --lib` — **1 passed, 0 failed**; finalny receipt zawiera zmierzone lifecycle/timing/count/workspace telemetry.
- `cargo test -p fullmag-runner 'fdm::cpu::spin_transport::tests' --lib` — **28 passed, 0 failed**.
- `cargo test -p fullmag-runner --test fdm_source_layout_contract` — **10 passed, 0 failed** (źródłowy kontrakt CUDA, bez wykonania device).
- `just verify-fem-dg0-step-metrics-contract` — **PASS**: managed `fem-gpu` CMake/CUDA build oraz runtime fixture pinned/non-magnetic i all-frozen; build tree na ext4 poza checkoutem.
- Managed `fem_exchange_contract` — **PASS**: consistent-mass projection dla regularnych i mieszanych domen P1 oraz PBC, setup-owned preconditioner/solver i reuse workspace; target uruchomiony w kontenerze `fem-gpu`.
- `just verify-fem-material-element-ms-contract` — kompilacja natywna oraz `fem_exchange_contract` przechodzą; cały przepis zatrzymuje się później na niezależnym strict GPU/Hypre demag (`strict FEM GPU demag failed to select Hypre device memory`).
- managed `fem_adaptive_dt_contract` — **PASS**: CMake/MFEM/CUDA zbudował i uruchomił kontrakt wykluczający frozen z embedded error oraz norm/rotation guard; błędny rozmiar maski kończy się fail-closed.
- `cargo test -p fullmag-ir fdm_projection_policy_round_trips_with_stable_realization_identity --lib` — **1 passed, 0 failed**;
- `cargo test -p fullmag-plan fdm_projected_rk_policy_is_resolved_into_backend_plan --lib` — **1 passed, 0 failed**;
- `cargo test -p fullmag-engine projected_rk_macrospin_has_qualified_temporal_order --lib` — **1 passed, 0 failed** (obserwowany rząd dla Heun/RK23/RK4/RK45);
- `cargo test -p fullmag-runner reference_runner_executes_and_provenances_resolved_projection_policy --lib` — **1 passed, 0 failed**;
- `uv run --project packages/fullmag-py pytest -q packages/fullmag-py/tests/test_api.py -k "projection_policy or script_export_preserves_per_magnet_fdm_grids"` — **2 passed, 0 failed**;
- `cargo check --workspace --all-targets` — **PASS** (ostrzeżenia istniejące wcześniej);
- `cargo test --workspace --no-run` — **nie kończy się na Windows**: linkowanie `fullmag-fem-sys` bez zarządzanego runtime zgłasza cztery brakujące symbole natywne; nie jest to błąd ścieżki projekcji.
- `just verify-fem-time-domain-native-contract` — **PASS**: wszystkie natywne targety FEM, kontrakt strict GPU, testy CUDA, macierz pochodnych, `fullmag-fem-sys` — **39 passed**, oraz testy Python — **8 passed**.
- `fem_rk_explicit_contract` w powyższej recepcie — **PASS**: rozbieżny accepted candidate wymusza endpoint refresh, a Brown thermal wybiera RHS ostatniego stage przy wyłączonym FSAL.
- `cargo +nightly test -p fullmag-runner --features fem-gpu native_fem::tests::native_fem_non_fsal_final_refresh_reuses_stepper_workspace -- --exact --nocapture` — **1 passed, 0 failed**; test potwierdza użycie prealokowanego checkpointu workspace.
- `fem_rk_transaction_fault_injection_contract` w powyższej recepcie — **PASS**; regresja potwierdza reuse workspace journala dla kolejnych transakcji oraz zachowanie telemetryki begin/rollback.
- `cargo +nightly test -p fullmag-runner --features fem-gpu native_fem::tests::native_fem_non_fsal_final_refresh_reuses_stepper_workspace -- --exact --nocapture` po zmianie PERF-002 — **1 passed, 0 failed**.

## Uzupełnienie remediacji runnera CPU

Po włączeniu regionalnych napędów do kanonicznego `ExchangeLlgProblem::observe()` usunięto wtórne doliczanie `drive_energy` w `crates/fullmag-runner/src/fdm/cpu/reference.rs`. `make_step_stats_from_report` i `observe_state_with_antenna_field` zachowują `e_drive` jako rozbicie, ale przekazują kanoniczne `e_ext`/`e_total`; helper energii używa lokalnego `M_s`, a rekonstrukcja `H_eff` dla nieaktywnych komórek uwzględnia napęd. Test `regional_drive_produces_distinct_field_and_energy_outputs` potwierdza `e_ext == e_drive == e_total` dla przypadku z samym napędem.

## Uzupełnienie remediacji FEM CPU

`fill_common_step_metrics` korzysta teraz z `max_cross_norm_aos_free`, który wspólnie uwzględnia maskę aktywnych węzłów magnetycznych i maskę frozen. Dzięki temu Airbox oraz przypięte węzły nie mogą zawyżyć kryterium stopu torque, a all-frozen magnetic set publikuje zero. Regresja `just verify-fem-dg0-step-metrics-contract` przeszła w managed kontenerze; pełna kwalifikacja adaptacyjnej normy FEM, parity i wydajności pozostaje otwarta.

`compute_adaptive_error_norm` i `compute_adaptive_attempt_guard_metric` otrzymują tę samą maskę frozen co ścieżka kroku RK i pomijają zamrożone węzły przed odczytem wartości. Produkcyjna ścieżka adaptive RK korzysta teraz z osobnej mass-weighted RMS opisanej niżej. Regresje z nie-finite frozen node oraz z niezgodnym rozmiarem maski przeszły w managed `fem_adaptive_dt_contract`; nadal otwarte są niezależny oracle, parity CPU/GPU i performance gate.

W `context_step_explicit_rk_mfem` cache ostatniego stage nie jest już utożsamiany z accepted endpoint bez sprawdzenia stanu po normalizacji/projekcji. Rozbieżny kandydat wykonuje jawny endpoint refresh i nie zasila kolejnego kroku FSAL; dla Brown thermal końcowa redukcja RHS czyta `k[last]`. Managed `fem_rk_explicit_contract` potwierdza oba scenariusze. Pełny kontrakt PERF-006 nadal wymaga telemetryki powodów refreshu, liczników operatorów, oracle i pomiaru steady-state.

## Uzupełnienie PERF-005

CPU Hypre Poisson publikuje `setup_count` i `fresh_zero_guess_count` w rzeczywistych granicach setup/apply; persistent `PoissonHypreWorkspace` przechowuje prealokowane wektory RHS/solution/residual, warm start oraz kopię ostatniego zaakceptowanego rozwiązania. Odrzucony kandydat albo wyjątek solve/certyfikacji przywraca tę kopię, zamiast kasować opublikowany warm start; bez wcześniejszego zaakceptowanego rozwiązania stan pozostaje nieważny. Periodiczny reduced CG ma ten sam transakcyjny lifecycle, prealokowany residual i jawne `iterative_mode`: pierwszy solve po setupie używa zera, sukces publikuje warm start, a późniejszy reject przywraca poprzedni zaakceptowany iterate. Teardown czyści liczniki razem z właścicielem cache. Dodatkowo CPU Poisson ma typowany `PoissonOperatorLifecycleReceipt` oraz content key obejmujący topologię/geometrię rzeczywistej siatki MFEM, rząd potencjału, członkostwo materiałowe, granice, PBC/gauge, realization Airbox, politykę solvera/AMG i urządzenie. Guard jest wykonywany przed RHS oraz przed decyzją o użyciu frozen-field cache; zmiana klucza unieważnia cache i stare workspace'y Hypre/PBC, kończąc publiczny apply błędem fail-closed.

Weryfikacja zarządzana: `fem_demag_poisson_contract` w obrazie `fem-gpu` z CUDA wyłączonym — PASS (`PERF005_POISSON_MANAGED_PASS`), w tym mutacja geometrii siatki, zmiana polityki Robin, `airbox_dirichlet_and_robin_match_manufactured_slab_oracle` (pełny produkcyjny solve warstwowego magnet–air dla Dirichleta i Robina, dokładny potencjał P2 oraz niezależna energia `mu0/162 J` w względnym progu `2e-10`), `periodic_demag_reuses_warm_start_and_resets_after_failure` (pierwszy reject → fresh zero, sukces → warm reuse, mniej iteracji dla niezmienionego RHS bez zmiany rozwiązania ponad względny próg `1e-12`, późniejszy reject → rollback zaakceptowanego warm startu i recovery bez fresh zero), `nonperiodic_hypre_rejected_candidate_preserves_published_warm_start` (mniej iteracji Hypre dla niezmienionego RHS przy tej samej zgodności rozwiązania, deterministyczny reject po zaakceptowanym solve, zachowanie warm startu i recovery bez setupu/fresh zero), `nonperiodic_poisson_reuses_setup_owned_workspace_for_repeated_solves` (100 solve, `setup_count == 1`, reuse=99, fresh zero=1; przez 99 wywołań po warm-up nie zmienia się żaden owner/workspace/operator/solver ani wskaźnik i capacity buforów pola), `failed_hypre_setup_does_not_publish_partial_state` (błędny preconditioner nie publikuje workspace/macierzy/solvera, a kolejny poprawny setup działa) oraz `failed_poisson_reinitialize_preserves_published_state` (nieudana reinitializacja zachowuje poprzedni operator i pozwala wykonać solve); sąsiednie `fem_exchange_contract` i `fem_mfem_context_contract` — PASS. Aktualny build i test kontraktu zakończyły się kodem 0 w zewnętrznym katalogu `D:\fullmag-fem-gpu-cpu-poisson-build`, a artefakty nie trafiły do indeksu Gita.

To nadal częściowa remediacja: pozostają physics oracle Fredkin–Koehler FEM-BEM, benchmark steady-state z transient-allocation i time-to-accuracy budgetem, jawnie zdefiniowana adaptive linear tolerance oraz kwalifikacja capability; analityczny oracle obejmuje już Airbox Dirichlet i Robin, fault-injection dowodzi rollbacku ostatniego zaakceptowanego rozwiązania, cold/warm porównanie dowodzi redukcji iteracji bez zmiany wyniku ponad zadany próg, a 100 powtórzonych solve dowodzi reuse stabilnego operatora i storage identity bez realokacji właścicielskich buforów. Nie zastępuje to pełnej macierzy stage'ów/integratorów ani pomiaru transient allocations wewnątrz MFEM/Hypre.

## Uzupełnienie PERF-006

CPU RK ma teraz typowany `EndpointCacheValidity` z osobnymi wymiarami `state`, `time`, `dynamic_sources`, `transport` i `projection`. Właściciel workspace zapisuje również `RkFinalRefreshReason`, liczbę końcowych ewaluacji RHS, dodatkowych solve Poissona, trafień cache, odświeżeń endpointu oraz czas zaakceptowanego kroku. Cache jest publikowany dopiero po zgodności znormalizowanego kandydata z ostatnim stage i weryfikacji czasu końcowego; callbacki Oersteda/transportu nie mogą zostać użyte jako domniemany FSAL między krokami bez osobnego zapytania o rewizję źródła. Brown pozostaje używalny dla bieżącego interwału, ale nadal blokuje FSAL następnego kroku.

Regresja `fem_rk_explicit_contract` w zarządzanym obrazie `fem-gpu` z `FULLMAG_ENABLE_CUDA=OFF` przeszła markerem `PERF006_ENDPOINT_TELEMETRY_PASS`: wszystkie cztery tableau sprawdzają operator/RHS counts, cache hit/refresh reason i accepted-step wall time; scenariusz rozbieżnego kandydata sprawdza dodatkowy endpoint RHS, a Brown sprawdza osobny zakaz FSAL. Build był podpięty z `D:\fullmag-fem-gpu-cpu-poisson-build`, poza checkoutem; override Compose usunięto, a `git ls-files` nie wykazuje artefaktów builda.

To nadal częściowa remediacja: publiczny ABI/provenance receipt jest już dostępny, ale brakuje niezależnego endpoint physics oracle, pełnej macierzy operator-count z demagiem, testu no-output i steady-state performance gate.

## Uzupełnienie PHY-001

Workspace DMI ma teraz jednego właściciela wejścia magnetyzacji dla PBC: `prepare_dmi_periodic_input` sprawdza rozmiar AoS, kompletność klas zredukowanych i zakres reprezentantów, a następnie wykonuje projekcję do kanonicznych wartości reprezentantów. iDMI i bulk DMI używają tego samego przygotowanego bufora; żaden z modułów nie utrzymuje własnej kopii polityki PBC.

Managed `fem_dmi_contract` i `fem_dmi_weak_residual` w obrazie `fem-gpu` z `FULLMAG_ENABLE_CUDA=OFF` zbudowały się i przeszły markerem `FEM_CPU_PHY001_PBC_DMI_PASS`. Build znajdował się w `D:\fullmag-fem-gpu-cpu-poisson-build`, poza checkoutem; tymczasowy override Compose został usunięty, a `git ls-files` nie wykazuje artefaktów builda.

To nadal częściowa remediacja: nie zamyka pełnego material membership/quad contract, Airbox zero-RHS/thermal oracle, manufactured PBC mode, fault-injection accepted state, CPU/GPU parity ani performance/provenance qualification.

## Uzupełnienie ARCH-001

Granica stanu AoS ma teraz typowany `AosVectorFieldView` dla lokalnych węzłów. Binding sprawdza `3*n_nodes`, kompletność i zakres mapy klas PBC oraz wymaga niezerowej rewizji mapy dla ścieżek publicznego stanu. `build_static_periodic_reduction` publikuje stabilny hash treści mapy jako `periodic_map_revision`; inicjalizacja stanu i upload magnetyzacji wykonują checked projection przed zmianą authoritative state.

Managed build w obrazie `fem-gpu` z CUDA wyłączonym zbudował i uruchomił `fem_aos_field_contract`, `fem_state_io_contract`, `fem_state_contract` oraz `fem_mesh_contract`, uzyskując marker `FEM_CPU_ARCH001_AOS_VIEW_PASS`. Build znajdował się w `D:\fullmag-fem-gpu-cpu-poisson-build`, poza checkoutem; tymczasowy override Compose usunięto, a indeks Gita nie zawiera artefaktów.

To nadal częściowa remediacja: nie ma pełnej migracji true/local/element/quadrature views ani gather/scatter maps w hot loop, representation receipts/provenance, Airbox oracle, elementowej nieciągłości materiału, CPU/GPU parity, fault-injection accepted state, metryk kopiowania ani performance/qualification gate.

## Uzupełnienie NUM-002

Planner FEM wylicza teraz konserwatywną estymację sztywności wymiany wyłącznie dla rozstrzygniętych komórek magnetycznych. `h_min` jest minimum odległości między węzłami komórki, a współczynniki `A` i `Ms` używają odpowiednio maksimum i minimum z pól DG0 lub nodalnych; dla zwykłego materiału używane są wartości skalarne. Zgodnie z konwencją native FEM estymata stosuje `omega_ex = 2 gamma_mu0 A / (mu0 Ms h_min^2)` oraz safety factor `0.1`.

Wynik jest publikowany w `ExecutionPlanIR.provenance.notes` razem z `h_min`, `A_max`, `Ms_min`, `omega_ex` i konserwatywnym limitem `dt`. Przy stałym kroku plan zapisuje, czy `dt` przekracza limit; w `execution_mode='strict'` jawny integrator explicit kończy plan błędem przed wykonaniem, natomiast `execution_mode='extended'` otrzymuje ostrzeżenie bez automatycznej zmiany integratora. Nie jest to jeszcze implementacja implicit/IMEX ani pełna polityka kwalifikacyjna.

Regresja `cargo test -p fullmag-plan fem_exchange_stiffness` przeszła **4/4** (dwa testy estymatora oraz reject/warning policy); target Cargo znajdował się w `D:\fullmag-plan-num002-build`, poza checkoutem. `git ls-files` nadal nie wykazuje artefaktów `target/build/dist/node_modules/.fullmag` ani binariów.

To nadal częściowa remediacja: nie ma jeszcze automatycznego wyboru explicit/implicit/IMEX, implicit/IMEX lifecycle, niezależnego stability oracle, telemetryki reject ratio/iteracji liniowych, testu refinement ani time-to-accuracy.

## Uzupełnienie NUM-001

Adaptive FEM CPU ma teraz osobną redukcję `compute_adaptive_error_norm_mass_weighted`. Dla każdego aktywnego i niefrozen węzła liczy ona wektorowy błąd względny względem `atol + rtol * max(||m_old||_2, ||m_new||_2)`, a następnie redukuje kwadrat przez dodatnie lumped mass/dual volume i dzieli przez sumę tej miary. Airbox oraz węzły nieaktywne lub frozen są pomijane przed odczytem danych; brak, ujemna, nie-finite albo źle długa miara kończy próbę fail-closed. Max spin rotation i max norm defect pozostają niezależnymi guardami.

`context_step_explicit_rk_mfem` wybiera najpierw `ctx.integration_weights.mfem_lumped_mass`, a przy pustym buforze kanoniczne `ctx.mesh.node_volumes`; nie ma cichego powrotu do nieważonego maksimum. Dotychczasowa funkcja maksimum pozostaje tylko jawnym kontraktem legacy/testowym. Kontrakty `fem_adaptive_dt_contract` oraz `fem_rk_explicit_contract` potwierdzają RMS, wykluczenie Airbox/frozen, odrzucenie uszkodzonej miary i użycie wag w produkcyjnej ścieżce RK; marker managed: `FEM_CPU_NUM001_RECEIPT_PASS`.

Każdy adaptacyjny `RkAttemptRecord` publikuje natywny receipt redukcji: typ normy (`mass_weighted_rms`), liczbę aktywnych węzłów, aktywną miarę, mianownik normalizacji, maksimum błędu skalowanego i RMS. Receipt jest zapisywany po każdej próbie, bez zmiany istniejących agregatów stałokrokowych. Additive ABI `fullmag_fem_backend_copy_solver_attempts_v2` przenosi te pola do runnera, JSON trace i `solver_attempts.csv`; ABI v1 pozostaje niezmienione. Managed kontrakty kompilacji, layoutu FFI i CSV przechodzą; pełny executable qualification nie dostarczył jeszcze receiptu, ponieważ zatrzymał się na istniejącym progu residualu demag po relaksacji.

To nadal częściowa remediacja: brak requested/resolved norm policy w authoring/IR/planner, pełnej telemetryki angular guard/stop reason, CPU/GPU reduction parity, niezależnego step-doubling/physics oracle, mesh-refinement invariance gate, benchmarku steady-state/time-to-accuracy oraz pełnej qualification capability.

## Otwarte findings

## Uzupełnienie PERF-007

Właściciel polityki wątków FEM CPU (`cpu_threads.*`) publikuje teraz typed receipt konfiguracji OpenMP: wyłączenie dynamicznego doboru zespołów, wyłączenie nested parallelism oraz limit jednej aktywnej warstwy równoległości. Ustawienia są stosowane przed rozgałęzieniem CPU/GPU i przed budową operatorów MFEM; log startowy zawiera te same decyzje, więc nie są one ukrytym fallbackiem. Gdy build ma `_OPENMP`, kontrakt odczytuje również wartości `omp_get_dynamic`, `omp_get_nested` i `omp_get_max_active_levels`.

Managed target `fem_cpu_threads_contract` w obrazie `fem-gpu` z `FULLMAG_ENABLE_CUDA=OFF` przeszedł markerem `PERF007_OPENMP_POLICY_PASS`. Build był podpięty z `D:\fullmag-fem-gpu-cpu-poisson-build`, poza checkoutem; tymczasowy Compose override został usunięty, a `git ls-files` nie wykazuje artefaktów builda.

To nadal częściowa remediacja: nie ma jeszcze wspólnej polityki affinity/NUMA dla runnera, osobnych i raportowanych limitów Hypre/AMG/BLAS, first-touch, testu wielosocketowego, benchmarku skalowania ani deterministycznej kwalifikacji time-to-accuracy.

Stan remediacji źródłowej/testowej wynosi 26/52 findingów (50,0%); pełna qualification pozostaje 0/52.

Otwarte pozostają przede wszystkim: kwalifikacja sprzętowa FDM/FEM GPU, pełna parity CPU/GPU materiałów–Airbox–PBC–DMI–termiki, device-side adaptive reductions, trwały lifecycle solverów Poissona/preconditionerów, matrix-free/partial assembly FEM, niezależny step-doubling/physics oracle dla projekcji RK, pre-projection telemetry, polityka precision/time-to-accuracy, CUDA Graphs oraz qualification CPU `Full`. Nie wolno oznaczać ich jako `PASS` na podstawie samego source testu.

Plik planu `docs/superpowers/plans/2026-08-25-c-drive-recovery-cleanup.md` został wcześniej dodany do commita `4315fe37a`; nie jest artefaktem generowanym ani buildem.

## Uzupełnienie PERF-001

CPU MFEM exchange publikuje teraz typowany `OperatorLifecycleReceipt` z kluczem zależności obejmującym topologię i geometrię kanonicznych buforów oraz rzeczywistej siatki MFEM, rząd FE, współczynniki materiałowe, maski i elementy brzegowe, dane PBC oraz realizację urządzenia. Setup publikuje klucz i liczniki dopiero po przygotowaniu całego operatora; nieudany setup nie nadpisuje poprzedniego stanu i zwiększa licznik błędów. Apply/reuse są liczone w ścieżce consistent-mass, a zmiana polityki planu unieważnia receipt i wymusza fail-closed przed kolejnym apply.

Weryfikacja zarządzana: kontener `fem-gpu`, target `fem_exchange_contract`, kompilacja MFEM/CUDA z buildem na `D:\fullmag-fem-gpu-cpu-poisson-build` oraz wykonanie zakończone markerami `PERF001_MESH_KEY_PASS` i `PERF001_PUBLIC_INVALIDATION_PASS`. Test `periodic_consistent_mass_projection_reuses_reduced_workspace` wykonuje 100 kolejnych apply na tym samym workspace i potwierdza `setup_count == 1` oraz ponowne użycie solvera. `failed_exchange_setup_does_not_publish_partial_state` wstrzykuje niepoprawną klasę PBC po assembly i potwierdza zachowanie poprzednich wskaźników, mas i metadanych oraz wzrost `failed_setup_count`. Build i runtime nie zostały zapisane ani zindeksowane w checkout.

To częściowa remediacja PERF-001: exchange ma już automatyczną detekcję zmiany klucza w publicznym runtime, kontrakt 100 kolejnych apply i regresję atomowego failed setup, ale nadal brakuje wspólnego lifecycle receipt dla demag/Poissona, pełnej macierzy fault-injection przebudowy operatora, niezależnego oracle, benchmarku setup/steady-state oraz qualification. Stan remediacji źródłowej/testowej wynosi 26/52 findingów (50,0%); pełna qualification pozostaje 0/52.

## Uzupełnienie FDM-CPU-QUAL-001

Commit `4bbbe58f518230d5ac5c201ccf8379af86fe39d3` publikuje wersjonowaną telemetrykę rzeczywiście wykonanych kroków `Minimal` i `Full` wraz z ich czasem ściennym. Produkcyjny harness z commita `b4a9f77c90b4bb115882467e8d517e1e8a182660` uruchamia małą, średnią i dużą fixture z demag, iDMI oraz dynamicznym polem przez publiczny ciąg `ProblemIR -> planner -> runner`; rozdziela tryby `Control`, `Requested` i `Full`, zapisuje requested/resolved/executed provenance, czasy planera i end-to-end, liczniki alokacji, FFT, kroków i outputów oraz hashe planu backendu i końcowej magnetyzacji. Wynik pozostaje jawnie oznaczony jako `evidence_only`.

Pierwszy publiczny smoke ujawnił dodatkowy mikrokrok, gdy suma sześciu kroków `1e-13 s` kończyła się o jeden ULP poniżej authored endpointu. Commit `8a65172f168e014c5853008002c450dfdc2faff7` zastąpił ścisłe porównanie czasu relatywną granicą roundoff i dodał test regresyjny `fixed_step_endpoint_roundoff_does_not_create_a_micro_step`; test przechodzi 1/1.

Release smoke został wykonany na dokładnym commicie `b4a9f77c90b4bb115882467e8d517e1e8a182660`, build identity `windows-x86_64-msvc-release-rustfft-6.4.1-realfft-3.5.0`. Zewnętrzny artefakt `D:\fullmag-builds\audit-fdm-cpu-qual001\release-smoke-b4a9f77c9-001\qualification-summary.json` ma SHA-256 `408B19FC5841A739A427893A4EBE551C6152EC7831EA8D9226A45C9DC8FAC8F4`. Dla tej samej siatki 40 komórek i sześciu kroków receipt potwierdza: `Control = 6 Minimal + 0 Full`, `Requested = 4 Minimal + 2 Full`, `Full = 0 Minimal + 6 Full`; każdy wariant wykonał 54 forward i 54 inverse FFT, nie miał rejectu ani fallbacku, a wszystkie trzy warianty mają identyczny hash planu `sha256:b185017a5314ffb37219e5a45ce0fbd887ed121b3d902321115c8427055f3da6` i identyczny hash końcowego stanu `sha256:be645624d6d215bded4509a979753a9603e4611f6a5e6d59a17e553cdf1f1766`. Testy `fullmag-bench` przechodzą 5/5. Build i dowody pozostały poza checkoutem i nie zostały zindeksowane przez Git.

Commit `a663fcf2145b6fa4ce5759efbf8889f764c5dda9` domyka brak samego pomiaru peak memory: globalny alokator zapisuje per-run peak przyrostu żywej sterty Rust, a suite zapisuje peak resident memory procesu przez Windows `PeakWorkingSetSize` albo Linux `VmHWM`; brak wspieranej metryki kończy harness błędem. Release evidence `D:\fullmag-builds\audit-fdm-cpu-qual001\release-peak-a663fcf21-001\qualification-summary.json` ma SHA-256 `03BC2D8252F7F28ACB8748F3B2C8264F9DF3AC1BB2E2DA20AC8852A0C10420B7`. Na tej maszynie peak resident wyniósł `10 215 424 B`, a peak przyrostu sterty odpowiednio `314 107 B`, `200 380 B` i `209 899 B` dla `Control`, `Requested` i `Full`. Testy `fullmag-bench` przechodzą 6/6; artefakt pozostaje poza checkoutem.

To nadal częściowa remediacja `FDM-CPU-QUAL-001`: brakuje niezależnego time-to-accuracy oracle, zatwierdzonych progów względem sprzętowego baseline'u oraz pełnego profilu wszystkich trzech rozmiarów fixture. Nie wolno promować capability na podstawie smoke. Stan remediacji źródłowej/testowej pozostaje 26/52 findingów (50,0%); pełna qualification pozostaje 0/52.

## Finalny gate FDM-CPU-QUAL-001 — 2026-08-27

Commity od `05e14ebca9a7bda5cc36e4ce484ffb5210066ed2` do
`2c7cd2d7810a513a132ced1cf83815d9f3ff64d1` dodały współdzielony niezależny oracle
stałego pola, pomiar time-to-accuracy, tożsamość sprzętu i toolchainu,
zatwierdzoną politykę progów oraz fail-closed verifier. Commit
`0e197b4ef6fd9eeabe727dc63d8eaaa20ca47b72` poprawił nieprawdziwy blocker
`hardware_baseline_threshold_not_approved`: pojedynczy run pozostaje
`evidence_only`, ale publikuje teraz rzeczywisty stan
`external_hardware_baseline_gate_pending`. Commit
`798eac3f242cde596ece8406cd16396b41842c4a` dopuszcza stary reason code
wyłącznie dla historycznego baseline'u i nadal odrzuca go po stronie candidate.
Testy verifiera przechodzą 6/6, a `fullmag-bench` 9/9.

Finalny release gate porównał po 20 przeplatanych powtórzeń czystego baseline'u
`1e14a306be52b68d181fd1a6936f165d66119ea8` i czystego candidate
`798eac3f242cde596ece8406cd16396b41842c4a`. Obie grupy użyły tego samego
`rustc 1.98.0`, targetu `x86_64-pc-windows-msvc` i fingerprintu sprzętu
`sha256:9da8191c93ff2a422576dd35ad2fad4c4818f4db0f02a00844f4b2c0659571f3`.
Candidate był związany ze snapshotem
`c5017c41d1da1dc21578461724061c4023a22b6194d3402b839bd5074cbc4ffc`.

Receipt
`D:\fullmag-builds\audit-fdm-cpu-qual001\gate-clean-1e14a306b-vs-798eac3f2-001.json`
ma SHA-256
`ACE370CAE038D0B30A4A240B16B89DFE7E30CB6FFF927DC3BAE865BEA356B943`
i status `passed`: 39/39 porównań przeszło, bez failure reason. Najwyższy p95
czasu end-to-end względem baseline'u wyniósł `1.110829661` przy limicie `1.25`;
p95 time-to-accuracy `1.025409368` przy limicie `1.25`; p95 peak RSS po
allowance `0.9536498405` przy limicie `1.15`. Pierwszy przechodzący krok czasu
pozostał `2.5e-11 s`, a obserwowany rząd zbieżności candidate wyniósł
`1.999240606`. Verifier potwierdził identyczne hashe planu i końcowej
magnetyzacji, dokładne CPU/double, brak fallbacku, 30 zaakceptowanych i zero
odrzuconych prób w każdej fixture oraz oczekiwane liczniki Minimal/Full i FFT.

Dowód zamyka brak oracle, progów, pełnej macierzy small/medium/large i
sprzętowego gate'u po stronie źródłowej/testowej `FDM-CPU-QUAL-001`. Stan tej
warstwy wynosi teraz 27/52 findingów (51,9%). Nie promowano jednak ogólnego
rejestru timestep capability: dowód dotyczy dokładnie
`fdm_cpu_reference_double_heun_full_fixture_v1`, natomiast istniejący wpis
`explicit_fixed_fdm_cpu_double` jest nadal związany z RK45 i wymaga osobnego,
zgodnego ze schematem artefaktu physics/production. Pełna qualification całego
findingu pozostaje więc otwarta, a globalny licznik pozostaje 0/52. Wszystkie
binaria, targety, 40 summary i finalny receipt pozostały poza checkoutem i nie
zostały zindeksowane przez Git.

## Uzupełnienie FDM-CPU-PERF-002 — 2026-08-27

Commit `f3c4e7ae8301aebc4b4f63e100589a9d72410495` usunął końcową regresję
alokacji powstałą przy rozbudowie receiptów accepted-state i snapshotów.
Metadane runu są teraz serializowane bezpośrednio do `BufWriter<File>` przez
typowaną projekcję `Serialize`, bez budowania pośredniego drzewa
`serde_json::Value` i drugiego pełnego `String`. Test parity porównuje pełną
projekcję nowej ścieżki z dotychczasowym typed JSON-em, po odjęciu wyłącznie
dwóch pól dodawanych przez runtime. Skupione testy finalnych metadanych przeszły
7/7, test realizacji transferu multilayer 1/1, a cały moduł
`artifacts::tests` zakończył się wynikiem 95 passed i 3 failed; te same trzy
awarie odtworzono na czystym wcześniejszym `52b7b17308297e542561d077365336b2f57e2db3`,
więc nie są regresją tej zmiany.

Czysty release candidate był związany z commitem
`f3c4e7ae8301aebc4b4f63e100589a9d72410495` i snapshotem źródeł
`90d916b5e9d540368f5ea0a8b8c6d142909903c4ab684cfabf61d25669180f5d`.
Finalny gate porównał 20 powtórzeń candidate z 20 powtórzeniami baseline'u
`1e14a306be52b68d181fd1a6936f165d66119ea8` na tym samym fingerprintcie
sprzętu. Receipt
`D:\fullmag-builds\audit-fdm-cpu-metadata-stream\gate-clean-1e14a306b-vs-f3c4e7ae8-001.json`
ma SHA-256
`308EA92F682C35F6B124D598C3866DD098B35CD8D84F5458E4B19A4B09AAEE6B`
i status `passed`: 39/39 porównań przeszło, bez failure reason. Najgorszy
stosunek mediany/p95 liczby alokacji wyniósł `0.989127325` przy limicie `1.02`,
bajtów alokacji `0.992965184` przy limicie `1.05`, a czasu end-to-end
`1.075522880` przy limicie p95 `1.25`. Peak RSS po allowance 1 MiB wyniósł
`0.950775969` dla mediany i `0.962657159` dla p95. Pierwszy przechodzący krok
time-to-accuracy pozostał identyczny (`2.5e-11 s`), a stosunek czasu wyniósł
`1.006501799` dla mediany i `0.901506247` dla p95.

To nadal częściowa remediacja `FDM-CPU-PERF-002`. Peak RSS, alokacje, sterta,
czas i time-to-accuracy mają powtarzalny gate, ale brakuje sprzętowej telemetryki
pasma pamięci z PMU. Testowana maszyna ma jeden socket i jeden węzeł NUMA, więc
remote NUMA reads nie są na niej fizycznie dostępne; kwalifikacja na maszynie
wielowęzłowej pozostaje otwarta. Stan remediacji źródłowej/testowej pozostaje
27/52 findingów (51,9%), a pełna qualification 0/52. Binaria, target Cargo,
40 wejściowych summary i finalny receipt pozostały poza checkoutem i nie zostały
zindeksowane przez Git.

## Uzupełnienie FDM-CPU-NUM-003 i FDM-GPU-NUM-003 — 2026-08-28

`FDM-CPU-NUM-003` jest źródłowo/testowo zremediowany jako jawny kontrakt
fixed-step: zmiana `dt` resetuje historię ABM3, a adaptive ABM3 jest odrzucany
przed plannerem. `FDM-GPU-NUM-003` ma teraz RED/GREEN dla diagnostycznego
priorytetu termiki/waveformu nad pustym cache'em oraz bounded managed-device
receipt dla RK23/DP45 FP64/FP32. Na RTX 3070 Laptop GPU wszystkie 12 przypadków
przeszło bez fallbacku i rollbacku; przebiegi deterministyczne oszczędziły
dokładnie 19 RHS na 20 kroków, a termika i dynamiczny Oersted nie użyły FSAL.

Bieżący stan remediacji źródłowej/testowej wynosi **29/52 findingów (55,8%)**.
Pełna globalna qualification pozostaje **0/52**: oba findingi zachowują jawnie
opisane granice dalszej kwalifikacji, a publiczny adaptive FDM GPU nadal ma
status `source_visible/unvalidated`. Wszystkie binaria, drzewa CMake i receipt
CUDA pozostały pod zewnętrznym wolumenem D:/WSL i nie są indeksowane przez Git.

## Uzupełnienie FDM-GPU-ABI-001 — 2026-08-28

Naprawiono także samą bramkę managed: wcześniej używała kontenerowej ścieżki
`/mnt/fullmag-zfn2-native` jako źródła bind mountu hosta. Na Windows omijało to
zatwierdzony wolumen WSL, `nvcc` nie był wykrywany, a CMake tworzył niekompletne
drzewo. Recepta przyjmuje teraz `FULLMAG_MANAGED_NATIVE_HOST_ROOT` wyłącznie po
stronie hosta i montuje je pod stałe `/mnt/fullmag-zfn2-native` w kontenerze.

Po poprawce pełny build CUDA 12.4, C++ sentinel, Rust layout i runner z feature
CUDA przeszły. Checked-v2 wykonał rzeczywisty krok na RTX 3070 Laptop GPU,
przeszedł analityczny oracle i opublikował receipt bez fallbacku. Bieżący stan
remediacji źródłowej/testowej wynosi **30/52 findingów (57,7%)**; pełna globalna
qualification pozostaje **0/52**. Wszystkie targety i binaria pozostały poza
checkoutem i indeksem Git.

## Uzupełnienie FDM-GPU-ARCH-001 — 2026-08-28

Receipt strict residency ma teraz schemat v2 i nie może zostać opublikowany bez
pełnego `source_commit` oraz SHA-256 całego diffu względem `HEAD`. Managed recipe
odrzuca bind mount, jeśli kontener nie widzi `/dev/loop*` z typem `ext4`, więc
efemeryczny overlay Docker Desktop nie może już udawać trwałego dowodu. Rzeczywisty
krok CUDA FP64/Heun przeszedł na RTX 3070 Laptop GPU z kompletnymi maskami
resolved/executed, zerowym fallbackiem, zerowymi pełnymi transferami i zerowym
host compute w hot loop. CTest przeszedł 1/1, testy runnera rezydencji 13/13, a
testy routingu 8/8.

Bieżący stan remediacji źródłowej/testowej wynosi **31/52 findingów (59,6%)**.
Pełna globalna qualification pozostaje **0/52**: receipt obejmuje tylko bounded
fixture FP64/Heun i nie zastępuje publicznego E2E, macierzy interakcji,
niezależnego oracle trajektorii ani time-to-accuracy. Buildy i trwały receipt
pozostają na zewnętrznym loop-backed ext4 i nie są indeksowane przez Git.

## Uzupełnienie FDM-GPU-TRX-001 — 2026-08-29

Produkcyjna granica pojedynczego kroku CUDA ma teraz jednorazowe, testowe
wstrzykiwanie awarii w fazach transakcji oraz pełny rollback snapshotu urządzenia,
historii ABM i publicznych statystyk. Retry termiczny zachowuje identyfikator
zaakceptowanego interwału RNG, a nowy test porównuje wynik po awarii i ponowieniu
bitowo z czystym przebiegiem kontrolnym. Wspólna polityka routingu ogranicza
conditional device graph do wspieranych kroków headless; pełne statystyki,
termika, dynamiczny Oersted i transport pozostają na ścieżce transakcyjnej.

Managed `just verify-fdm-gpu-fsal-contract` przeszedł oba testy CTest oraz 16
przypadków runtime na RTX 3070 Laptop GPU: RK23/DP45 × FP32/FP64 × przebieg
deterministyczny, termiczny, termiczny retry replay i dynamiczny Oersted.
Wszystkie przypadki wykonały `cuda_fdm` bez fallbacku; przebiegi deterministyczne
odzyskały FSAL po pierwszym kroku, a retry termiczny zachował dokładny stan
zaakceptowany i nie opublikował statystyk z nieudanej próby.

Jest to częściowa remediacja `FDM-GPU-TRX-001`. Pozostają runtime fault-injection
dla każdej wewnętrznej fazy RK i sprzężonego transportu, sanitizer, pełny test
checkpoint/restart, szeroka macierz interakcji oraz gate time-to-accuracy.
Bieżący stan remediacji źródłowej/testowej wynosi **32/52 findingów (61,5%)**;
pełna globalna qualification pozostaje **0/52**. Buildy, drzewa CMake i dowód JSON
pozostały na zewnętrznym loop-backed ext4 i nie są indeksowane przez Git.

## Uzupełnienie FEM-GPU-TRX-001 — 2026-08-29

Transakcja kroku RK na FEM CUDA kopiuje teraz wyłącznie autorytatywną
magnetyzację oraz stan FSAL `k0`, zamiast trzynastu pól wektorowych i dwóch
rozwiązań Poissona. Dla jednowęzłowego kontraktu wykonywany ładunek capture lub
restore spadł z 328 B do 48 B. Po rollbacku pola pochodne zaakceptowanego końca
kroku są jawnie unieważniane, ich bezpośredni odczyt kończy się fail-closed do
czasu odświeżenia snapshotu, a następne rozwiązanie demagnetyzacji wymaga
świeżego przybliżenia początkowego Poissona. Commit kroku ponownie oznacza
obserwable jako ważne, natomiast zewnętrzny upload magnetyzacji je unieważnia.

Oddzielna transakcja bezpośredniego minimizera/relaksacji zachowuje na tym etapie
pełny legacy rollback pól i rozwiązań Poissona. Managed recipe na loop-backed
ext4 zbudowała kod przy CUDA 12.4 i uruchomiła trzy wykonywane kontrakty:
`fem_cuda_rk_guard_contract`, `fem_source_facade_gpu_rk_contract` oraz
`fem_rk_transaction_fault_injection_contract`; wszystkie 3/3 przeszły.

Jest to częściowa remediacja `FEM-GPU-TRX-001`, dlatego licznik nie zmienia się:
stan remediacji źródłowej/testowej pozostaje **32/52 findingów (61,5%)**, a pełna
globalna qualification **0/52**. Pozostają migracja pełnych buforów rollbacku
relaksacji i odzyskanie ich VRAM, produkcyjne fault-injection dla każdej fazy,
pełne oracle fizyczne/parytetowe oraz gate wydajności i time-to-accuracy.
Wszystkie binaria i drzewa CMake pozostały na zewnętrznym loop-backed ext4 i nie
są indeksowane przez Git.

## Domknięcie źródłowe/testowe FEM-GPU-TRX-001 — 2026-08-29

Usunięto ostatnią legacy realizację pełnego rollbacku z nonlinear-CG. RK
workspace nie alokuje już jedenastu zapasowych pól H ani zapasowych rozwiązań
Poissona. Trwały koszt VRAM transakcji zmniejszył się dokładnie o
`264 × N + 8 × (S + F)` B, gdzie `N` jest liczbą węzłów, `S` liczbą skalarnych
DOF rozwiązania, a `F` liczbą pełnych skalarnych DOF. Jedyny journal RK ma teraz
`48 × N` B i obejmuje wyłącznie magnetyzację oraz endpoint FSAL `k0`.

Nonlinear-CG wykorzystuje istniejący `m_backup` i kopiuje kierunek PR+ tylko,
gdy jest ważny. Rzeczywisty rollback synchronizuje stream dopiero po awarii,
przywraca bitowo magnetyzację, kierunek i metadane zaakceptowanego kroku,
unieważnia pola pochodne, token zaakceptowanej ewaluacji i FSAL oraz wymusza
świeży guess Poissona. Dzięki invalidacji tokenu retry nie może użyć stale
`H_eff`. Dodano one-shot fault-injection po publikacji trial magnetization i
w trakcie finalizacji zaakceptowanych statystyk; dla obu punktów retry daje
bitowo ten sam stan i obserwable co czysty przebieg kontrolny.

Managed `just verify-fem-gpu-rk-transaction-contract` na CUDA 12.4 i
loop-backed ext4 przeszedł **4/4 CTest**. Wykonywany mixed-P1 potwierdził PGBB
oraz dwa kolejne kroki nonlinear-CG dla `exchange_only` i `device_hypre`,
z ważnym dowodem Armijo, zerowym H2D w hot loop oraz istniejącymi budżetami
kontrolnych D2H/synchronizacji. Osobny wykonywany test alokatora potwierdził
dokładny budżet minimalnego workspace.

`FEM-GPU-TRX-001` jest zatem zremediowany źródłowo/testowo. Bieżący stan wynosi
**34/52 findingów (65,4%)**; pełna globalna qualification pozostaje **0/52**.
Do kwalifikacji capability nadal potrzebne są hash-bound publiczne E2E,
szersza macierz interakcji/integratorów, sanitizer i OOM fault-injection oraz
sprzętowy gate time-to-accuracy dla dokładnie promowanego tuple. Wszystkie
buildy i drzewa CMake pozostały poza checkoutem i indeksem Git.

## Domknięcie źródłowe/testowe FDM-GPU-TRX-001 — 2026-08-29

Wcześniejsza remediacja obejmowała wyłącznie single-grid. Audyt produkcyjnego
call graphu ujawnił drugą publiczną realizację: `fullmag_fdm_backend_step` dla
multilayer wywoływał integrator bez `StepTransactionController`. RED reproducer
potwierdził błąd — fault final-stats zwracał sukces zamiast odrzucić próbę.

Multilayer korzysta teraz z tej samej sekwencji transakcyjnej co single-grid.
Każda warstwa ma prealokowany snapshot accepted magnetization, capture i restore
są D2D, a commit czasu, `dt`, `step_count`, `accepted_step_index` i revision jest
odroczony do granicy accepted commit. Po błędzie magnetyzacja wraca bitowo,
derived observables są odtwarzane, statystyki wywołującego pozostają bez zmian,
a retry daje bitowo ten sam endpoint co czysty krok kontrolny.

Managed `just verify-fdm-gpu-transaction-contract` na RTX 3070 Laptop GPU
przeszedł CTest **3/3**, dwa dokładne testy runnera **2/2** oraz termiczny
RK23/DP45 oracle/retry z hash-bound provenance i zerowym fallbackiem. Macierz
multilayer obejmuje Heun/RK4/RK23 × FP32/FP64 oraz fault integratora,
final-stats, receipt i transport-commit. Checkpoint/restart przechodzi bitowo dla
przypadku deterministycznego i Brown thermal. `just verify-fdm-gpu-abi-contract`
przeszedł Rust layout **2/2**, C sentinel **1/1** i pełny CUDA runner check.

Compute Sanitizer pozostaje jawnie niezaliczony: WDDM odmówił inicjalizacji,
ponieważ systemowe klucze debuggera NVIDIA są wyłączone. Test pod sanitizerem
wykonał się i zwrócił PASS, ale sam memcheck/synccheck raportował dwa błędy
inicjalizacji, więc nie jest to dowód sanitizera. Włączenie interfejsu wymaga
osobnej zgody na zmianę rejestru HKLM albo wykonania bramki na natywnym hoście
Linux/CI.

`FDM-GPU-TRX-001` jest zremediowany źródłowo/testowo. Bieżący stan wynosi
**35/52 findingów (67,3%)**; pełna globalna qualification pozostaje **0/52**.
Do kwalifikacji capability nadal potrzebne są zaliczony sanitizer, szeroki
publiczny authoring→IR→planner→runner E2E, transport-coupled matrix i sprzętowy
gate time-to-accuracy. Wszystkie buildy, targety Cargo i JSON evidence pozostały
poza checkoutem i indeksem Git.

## Rozszerzenie remediacji FEM-CPU-ARCH-001 — 2026-08-29

RED wykazał dwa rzeczywiste defekty reprezentacji. Planner przekazuje maskę
frozen-spins indeksowaną lokalnymi węzłami siatki, lecz builder przy PBC
walidował jej długość jako liczbę zredukowanych true nodes. Następnie runtime
indeksował tę maskę bezpośrednio względem lokalnego `ctx.state.m_xyz`. Ponadto
produkcyjne projekcje PBC miały wariant `void`, który przy uszkodzonej mapie albo
złym extent po cichu nie wykonywał projekcji.

Builder importuje teraz frozen-spins przez jeden adapter local/true. Adapter
sprawdza rewizję i reprezentantów klas, identyczne membership/reference w całej
klasie PBC oraz odrzuca nieaktywne węzły Airbox. Cicha projekcja została usunięta;
RK, relaxation, DMI, effective-field i upload używają wyłącznie wariantu
fail-closed. `RepresentationAuditCounters` mierzy liczbę kopii, dokładne bajty
odczytu/zapisu gather-scatter, invalid-space assertions i liczniki hot-loop.
Przy okazji rozszerzony kontrakt ujawnił stale-buffer w kompozycji `H_eff`:
wyłączone anisotropy/DMI mogły pozostawić poprzednią zawartość. Wszystkie cztery
lokalne bufory są teraz jawnie zerowane przed kompozycją.

Publiczny `fullmag_fem_representation_receipt_v1` ma zamrożony, addytywny ABI
88 B i publikuje executed local-node AoS, lokalizacje `Ms`/`A`, local/true
extents, rewizję PBC oraz kumulatywną telemetrykę konwersji. Rust FFI waliduje
wersję, rozmiar, enumy i wewnętrzne inwarianty; runner dołącza typed receipt do
każdego natywnego `StepStats`. Rzeczywisty krok exchange-only potwierdza drogę
native backend → C ABI → Rust FFI → publiczny, serializowalny wynik → trwałe
`StepDiagnostics` artefaktu.

Managed `just verify-fem-cpu-representation-contract` przeszedł native CTest
**13/13**, layout ABI **1/1**, runner receipt **3/3**, diagnostyka/serializacja
**1/1**, `rustfmt` i pełny runner check na buildzie C++/CUDA. Artefakty CMake i
Cargo pozostały w
`/mnt/fullmag-zfn2-native/fem-cpu-representation`, czyli na loop-backed ext4 poza
checkoutem i indeksem Git. Finding pozostaje częściowy i nie zwiększa licznika
**36/52 (69,2%)**: nadal brakuje pełnego round-trip MFEM local↔true, CPU/GPU
parity, fault-injection całego kroku oraz qualification/time-to-accuracy.

## Domknięcie źródłowe/testowe FEM-CPU-PERF-003 — 2026-08-29

Produkcja przygotowywała już minimalny, zeropayloadowy attempt checkpoint w
builderze kontekstu, lecz `context_step_explicit_rk_mfem()` zachowywał drugą,
niejawną realizację i konstruował `fallback_attempt_cache`, gdy setup nie
dostarczył obiektu. RED kontrakt wykazał dokładnie tę ścieżkę.

Fallback został usunięty. Adaptive RK23/RK45 używa wyłącznie setup-owned
`StepperWorkspace::attempt_checkpoint`; brak checkpointu kończy się fail-closed
przed pętlą prób. Wymuszone retry potwierdza stałą tożsamość obiektu, zero
alokacji checkpointu w hot loop, zero bajtów capture/restore, capture per attempt
i restore per reject oraz endpoint zgodny z niezależnym oracle RK. Profilowanie
jest teraz odczytywane dynamicznie przy capture/restore, więc włączenie go po
setupie nie gubi telemetrii prób.

Managed `just verify-fem-gpu-rk-transaction-contract` przeszedł CTest **5/5**,
`fullmag-fem-sys` **41/41**, runner check i mixed-P1 rollback/transfer budgets.
`FEM-CPU-PERF-003` jest zremediowany źródłowo/testowo. Bieżący stan wynosi
**36/52 findingów (69,2%)**; pełna globalna qualification pozostaje **0/52**.
Pozostają hash-bound publiczny E2E/provenance i osobny benchmark reject
overhead/time-to-accuracy. Build CMake i Cargo pozostał na loop-backed ext4,
poza checkoutem i indeksem Git.
