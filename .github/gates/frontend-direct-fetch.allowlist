# Signature format:
#   <path>:<matched_fragment>
#
# Keep only intentional canonical fetch boundaries here.
# LiveSessionClient is the single OpenAPI transport boundary; React code must
# continue to use resource hooks/client modules instead of direct fetch().

apps/web/src/api/client/LiveSessionClient.ts:fetch(
