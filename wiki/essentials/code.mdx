---
title: 'Code blocks'
description: 'How to format commands and code examples in the wiki'
icon: 'code'
---

## Inline code

Use backticks for commands, filenames, and identifiers.

```
To denote a `word` or `phrase` as code, enclose it in backticks (`).
```

## Code blocks

Use fenced code blocks with a language tag for syntax highlighting.

```java HelloWorld.java
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, Fullmag!");
    }
}
```

````md
```java HelloWorld.java
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```
````
