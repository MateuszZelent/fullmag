import { FieldRow } from "../../primitives/FieldRow";

export function FrequencyDomainModalMarkerRows() {
  return (
    <FieldRow
      label="Modal semantics"
      value="eigen frequencies remain modal markers; they are not FMR peaks"
    />
  );
}
